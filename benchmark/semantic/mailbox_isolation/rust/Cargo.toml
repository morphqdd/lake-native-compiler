[package]
name = "mailbox_isolation"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt", "macros", "sync", "io-util", "io-std"] }

[profile.release]
lto = true
codegen-units = 1
strip = true
